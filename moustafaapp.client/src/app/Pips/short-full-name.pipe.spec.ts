import { ShortFullNamePipe } from './short-full-name.pipe';

describe('ShortFullNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortFullNamePipe();
    expect(pipe).toBeTruthy();
  });
});
